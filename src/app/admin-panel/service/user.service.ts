import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import {getAdminUrl} from "../../url.config";

@Injectable({
  providedIn: 'root'
})
export class UserService {

}
